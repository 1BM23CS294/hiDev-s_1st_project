/**
 * Defines the list of guest users who have read-only access to all analysis reports.
 *
 * IMPORTANT: Add the Google account emails of your guests to this list.
 * Only users who sign in with these exact email addresses will be granted guest access.
 */
export const guestEmails: string[] = [
  // Example: 'guest-viewer@example.com',
];
